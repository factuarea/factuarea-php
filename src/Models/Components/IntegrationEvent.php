<?php

/**
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

declare(strict_types=1);

namespace Factuarea\Sdk\Models\Components;


/** IntegrationEvent - One event a payment gateway sent to Factuarea, with the outcome it produced and, when it produced none, the typed reason why. Read-only and scoped to your own company. The raw content of the event is NEVER exposed: it carries personal data of your end customers and payment details, is stored encrypted only for parked events so they can be reprocessed, and is purged 30 days after being parked. */
class IntegrationEvent
{
    /**
     * UUID v7 of the event. Use it in `GET /v1/companies/{company}/integrations/events/{event}` and in the replay operation.
     *
     * @var string $id
     */
    #[\Speakeasy\Serializer\Annotation\SerializedName('id')]
    public string $id;

    /**
     * Always `integration_event`.
     *
     * @var \Factuarea\Sdk\Models\Components\IntegrationEventObject $object
     */
    #[\Speakeasy\Serializer\Annotation\SerializedName('object')]
    #[\Speakeasy\Serializer\Annotation\Type('\Factuarea\Sdk\Models\Components\IntegrationEventObject')]
    public IntegrationEventObject $object;

    /**
     * Gateway or integration the event came from. Closed set, so it is safe to switch on it.
     *
     * @var \Factuarea\Sdk\Models\Components\IntegrationEventProvider $provider
     */
    #[\Speakeasy\Serializer\Annotation\SerializedName('provider')]
    #[\Speakeasy\Serializer\Annotation\Type('\Factuarea\Sdk\Models\Components\IntegrationEventProvider')]
    public IntegrationEventProvider $provider;

    /**
     * Event type as reported by the provider, matched EXACTLY when you filter by it. Free-form text, NOT a closed enum: the field deliberately mixes the raw provider type (`charge.refunded`, `invoice.paid`) with the internal semantic value some older branches recorded (`autoinvoice.*`). Do not model it as a fixed catalogue — for a filterable, closed axis use `discard_reason`.
     *
     * @var string $eventType
     */
    #[\Speakeasy\Serializer\Annotation\SerializedName('event_type')]
    public string $eventType;

    /**
     * Direction of the exchange: `inbound` for something the provider sent us (all gateway webhooks), `outbound` for a call the platform made to the provider, `internal` for a state change of the integration itself. Free-form string rather than an enum: it is not backed by a closed catalogue in code.
     *
     * @var string $direction
     */
    #[\Speakeasy\Serializer\Annotation\SerializedName('direction')]
    public string $direction;

    /**
     * Outcome of the event: `success` (it produced its effect), `skipped` (it was discarded on purpose — see `discard_reason`) or `failure` (it broke while being processed — see `error_message`).
     *
     * @var \Factuarea\Sdk\Models\Components\IntegrationEventStatus $status
     */
    #[\Speakeasy\Serializer\Annotation\SerializedName('status')]
    #[\Speakeasy\Serializer\Annotation\Type('\Factuarea\Sdk\Models\Components\IntegrationEventStatus')]
    public IntegrationEventStatus $status;

    /**
     * `true` when the account owner can do something about this discard (turn a setting on, re-link an account). Only actionable discards notify the user; the informative ones are recorded but never interrupt anyone.
     *
     * @var bool $isActionable
     */
    #[\Speakeasy\Serializer\Annotation\SerializedName('is_actionable')]
    public bool $isActionable;

    /**
     * CONTRACT of the replay operation: when it is `true`, `POST /v1/companies/{company}/integrations/events/{event}/replay` does not answer 422. It is the conjunction of three conditions — the event is parked, it still holds its content, and its discard reason admits reprocessing — evaluated by the same handler that guards the replay. It turns to `false` on its own once the 30-day retention window purges the content, even though the event stays parked.
     *
     * @var bool $isReplayable
     */
    #[\Speakeasy\Serializer\Annotation\SerializedName('is_replayable')]
    public bool $isReplayable;

    /**
     * When the event was recorded (ISO 8601).
     *
     * @var \DateTime $createdAt
     */
    #[\Speakeasy\Serializer\Annotation\SerializedName('created_at')]
    public \DateTime $createdAt;

    /**
     * Typed reason why the event was discarded, from a CLOSED catalogue, or `null` when it was not discarded. This is the axis to filter and group by. A value retired from the catalogue in a later version is still returned verbatim here, but its `discard_reason_label`, `is_actionable` and `is_replayable` degrade to neutral rather than breaking the page.
     *
     * @var ?\Factuarea\Sdk\Models\Components\IntegrationEventDiscardReason $discardReason
     */
    #[\Speakeasy\Serializer\Annotation\SerializedName('discard_reason')]
    #[\Speakeasy\Serializer\Annotation\Type('\Factuarea\Sdk\Models\Components\IntegrationEventDiscardReason|null')]
    public ?IntegrationEventDiscardReason $discardReason;

    /**
     * Human-readable label of `discard_reason`, in Spanish (the platform's end-user language). `null` when there is no discard reason, or when the stored reason is no longer part of the current catalogue.
     *
     * @var ?string $discardReasonLabel
     */
    #[\Speakeasy\Serializer\Annotation\SerializedName('discard_reason_label')]
    public ?string $discardReasonLabel;

    /**
     * Error raised while processing the event, or `null` when there was none. Relevant for `failure`; a `skipped` event carries its reason in `discard_reason`, not here.
     *
     * @var ?string $errorMessage
     */
    #[\Speakeasy\Serializer\Annotation\SerializedName('error_message')]
    public ?string $errorMessage;

    /**
     * Server-side processing time in milliseconds, or `null` when it was not measured.
     *
     * @var ?int $durationMs
     */
    #[\Speakeasy\Serializer\Annotation\SerializedName('duration_ms')]
    public ?int $durationMs;

    /**
     * Next step for this specific discard reason, in Spanish and in the imperative. Present ONLY in the detail and in the 202 of the replay — the listing omits it, because repeating the same sentence across 100 elements is noise. `null` when the reason is merely informative: if there is nothing to do, the contract does not invent an instruction. The sentence tells reproducible reasons ("… y reprocesa el evento") apart from the ones that are not ("… emítela a mano"), so it never points you at a call that would answer 422. When a reason admits BOTH ways out, they are worded as mutually exclusive and the sentence spells out the consequence of doing both: replay idempotency keys on the charge and only recognises invoices issued through that same automatic path, so an invoice created by hand does not stop it — doing both leaves two invoices for one charge, each numbered in its series and filed with VeriFactu.
     *
     * @var ?string $recommendedAction
     */
    #[\Speakeasy\Serializer\Annotation\SerializedName('recommended_action')]
    #[\Speakeasy\Serializer\Annotation\SkipWhenNull]
    public ?string $recommendedAction = null;

    /**
     * @param  string  $id
     * @param  \Factuarea\Sdk\Models\Components\IntegrationEventObject  $object
     * @param  \Factuarea\Sdk\Models\Components\IntegrationEventProvider  $provider
     * @param  string  $eventType
     * @param  string  $direction
     * @param  \Factuarea\Sdk\Models\Components\IntegrationEventStatus  $status
     * @param  bool  $isActionable
     * @param  bool  $isReplayable
     * @param  \DateTime  $createdAt
     * @param  ?\Factuarea\Sdk\Models\Components\IntegrationEventDiscardReason  $discardReason
     * @param  ?string  $discardReasonLabel
     * @param  ?string  $errorMessage
     * @param  ?int  $durationMs
     * @param  ?string  $recommendedAction
     * @phpstan-pure
     */
    public function __construct(string $id, IntegrationEventObject $object, IntegrationEventProvider $provider, string $eventType, string $direction, IntegrationEventStatus $status, bool $isActionable, bool $isReplayable, \DateTime $createdAt, ?IntegrationEventDiscardReason $discardReason = null, ?string $discardReasonLabel = null, ?string $errorMessage = null, ?int $durationMs = null, ?string $recommendedAction = null)
    {
        $this->id = $id;
        $this->object = $object;
        $this->provider = $provider;
        $this->eventType = $eventType;
        $this->direction = $direction;
        $this->status = $status;
        $this->isActionable = $isActionable;
        $this->isReplayable = $isReplayable;
        $this->createdAt = $createdAt;
        $this->discardReason = $discardReason;
        $this->discardReasonLabel = $discardReasonLabel;
        $this->errorMessage = $errorMessage;
        $this->durationMs = $durationMs;
        $this->recommendedAction = $recommendedAction;
    }
}