<?php

/**
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

declare(strict_types=1);

namespace Factuarea\Sdk\Models\Components;


/** StorefrontRevalidation - The answer to "is my cart still valid?": the cart already brought up to date plus the verdict line by line. It carries the WHOLE cart —with the same shape its own read has— so the shop window can carry on without a second call, which is what makes it usable right before paying. The two summary flags are derived from the lines so a client does not have to walk them to know whether to warn the buyer. */
class StorefrontRevalidation
{
    /**
     * Always `storefront_revalidation`. It opens the body because the result of revalidating has no identity of its own.
     *
     * @var \Factuarea\Sdk\Models\Components\StorefrontRevalidationObject $object
     */
    #[\Speakeasy\Serializer\Annotation\SerializedName('object')]
    #[\Speakeasy\Serializer\Annotation\Type('\Factuarea\Sdk\Models\Components\StorefrontRevalidationObject')]
    public StorefrontRevalidationObject $object;

    /**
     * The cart of an anonymous buyer: what they have picked, what it added up to the last time the server priced it, and who they say they are. Its identifier is unguessable and IS the only authorisation over it: whoever holds it can read and change it, and nobody else can. It does NOT hold stock — nothing is set aside for it — so what it shows can stop fitting, and that is what the revalidation is for. Expiry is NOT one of its statuses: it is derived from the expiry instant against the clock of the read, so no scheduled sweep is needed to make a cart stale.
     *
     * @var \Factuarea\Sdk\Models\Components\StorefrontSession $session
     */
    #[\Speakeasy\Serializer\Annotation\SerializedName('session')]
    #[\Speakeasy\Serializer\Annotation\Type('\Factuarea\Sdk\Models\Components\StorefrontSession')]
    public StorefrontSession $session;

    /**
     * Detail line by line, in the order of the cart. There is one entry per line, including the ones nothing happened to.
     *
     * @var array<\Factuarea\Sdk\Models\Components\StorefrontRevalidationLine> $lines
     */
    #[\Speakeasy\Serializer\Annotation\SerializedName('lines')]
    #[\Speakeasy\Serializer\Annotation\Type('array<\Factuarea\Sdk\Models\Components\StorefrontRevalidationLine>')]
    public array $lines;

    /**
     * Whether any line changed its amount. It is the flag a shop window hangs the "prices have changed" notice on.
     *
     * @var bool $hasChanges
     */
    #[\Speakeasy\Serializer\Annotation\SerializedName('has_changes')]
    public bool $hasChanges;

    /**
     * Whether every line still has enough units. In `false` the order will be rejected naming the first line that does not fit, so it is better to say so here.
     *
     * @var bool $allSufficient
     */
    #[\Speakeasy\Serializer\Annotation\SerializedName('all_sufficient')]
    public bool $allSufficient;

    /**
     * @param  \Factuarea\Sdk\Models\Components\StorefrontRevalidationObject  $object
     * @param  \Factuarea\Sdk\Models\Components\StorefrontSession  $session
     * @param  array<\Factuarea\Sdk\Models\Components\StorefrontRevalidationLine>  $lines
     * @param  bool  $hasChanges
     * @param  bool  $allSufficient
     * @phpstan-pure
     */
    public function __construct(StorefrontRevalidationObject $object, StorefrontSession $session, array $lines, bool $hasChanges, bool $allSufficient)
    {
        $this->object = $object;
        $this->session = $session;
        $this->lines = $lines;
        $this->hasChanges = $hasChanges;
        $this->allSufficient = $allSufficient;
    }
}