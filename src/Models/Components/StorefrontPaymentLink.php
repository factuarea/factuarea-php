<?php

/**
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

declare(strict_types=1);

namespace Factuarea\Sdk\Models\Components;


/** StorefrontPaymentLink - The payment link issued for an order: where to send the buyer, until when it works and whether the effect was simulated. Asking twice for the link of the same order does NOT create two payment sessions while the first one is still live: the second call returns the same address. The identifier of the gateway session is deliberately NOT published — the storefront receives it from the gateway itself on the return, and putting it in a body the browser stores would publish exactly what legitimises confirming a collection. */
class StorefrontPaymentLink
{
    /**
     * Address of the payment session to send the buyer to. Always over a secure scheme: the value object of the issued link rejects anything else.
     *
     * @var string $url
     */
    #[\Speakeasy\Serializer\Annotation\SerializedName('url')]
    public string $url;

    /**
     * Instant (ISO 8601) until which that address keeps working. It stays BELOW the life the gateway gives its own session, because publishing a longer validity would promise an address that no longer exists.
     *
     * @var \DateTime $expiresAt
     */
    #[\Speakeasy\Serializer\Annotation\SerializedName('expires_at')]
    public \DateTime $expiresAt;

    /**
     * Whether the effect was SIMULATED because the company is in test mode. It is part of the published contract and not an implementation detail: a simulated link is not a link that is missing, it is a link that exists and does not charge, so the storefront can say "this was a simulation" instead of deducing test mode from a gap. In a company that is not in test mode it travels `false` and never disappears.
     *
     * @var bool $simulated
     */
    #[\Speakeasy\Serializer\Annotation\SerializedName('simulated')]
    public bool $simulated;

    /**
     * @param  string  $url
     * @param  \DateTime  $expiresAt
     * @param  bool  $simulated
     * @phpstan-pure
     */
    public function __construct(string $url, \DateTime $expiresAt, bool $simulated)
    {
        $this->url = $url;
        $this->expiresAt = $expiresAt;
        $this->simulated = $simulated;
    }
}