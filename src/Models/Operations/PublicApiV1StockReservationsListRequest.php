<?php

/**
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

declare(strict_types=1);

namespace Factuarea\Sdk\Models\Operations;

use Brick\DateTime\LocalDate;
use Factuarea\Sdk\Utils\SpeakeasyMetadata;
class PublicApiV1StockReservationsListRequest
{
    /**
     *
     * @var string $company
     */
    #[SpeakeasyMetadata('pathParam:style=simple,explode=false,name=company')]
    public string $company;

    /**
     * Public ID of the holder, exactly the one sent when the goods were reserved. It is not checked against any table — which table it points at depends on the holder type — and everything stays scoped to the company in the path. Exact match on `holder_id`.
     *
     * @var ?string $holderId
     */
    #[SpeakeasyMetadata('queryParam:style=form,explode=true,name=holder_id')]
    public ?string $holderId = null;

    /**
     * Public ID of the product the goods are held against. A product that does not belong to the company in the path is rejected exactly like one that does not exist. Exact match on `product_id`.
     *
     * @var ?string $productId
     */
    #[SpeakeasyMetadata('queryParam:style=form,explode=true,name=product_id')]
    public ?string $productId = null;

    /**
     * Public ID of the variant the goods are held against, with the same rules as the product. Exact match on `variant_id`.
     *
     * @var ?string $variantId
     */
    #[SpeakeasyMetadata('queryParam:style=form,explode=true,name=variant_id')]
    public ?string $variantId = null;

    /**
     * Public ID of the warehouse the goods are held at, with the same rules as the product. An archived warehouse is accepted here: what it holds is still on hold. Exact match on `warehouse_id`.
     *
     * @var ?string $warehouseId
     */
    #[SpeakeasyMetadata('queryParam:style=form,explode=true,name=warehouse_id')]
    public ?string $warehouseId = null;

    /**
     * Cursor for forward pagination. Use the `uuid` of the last object on the previous page.
     *
     * @var ?string $startingAfter
     */
    #[SpeakeasyMetadata('queryParam:style=form,explode=true,name=starting_after')]
    public ?string $startingAfter = null;

    /**
     * Cursor for backward pagination. Use the `uuid` of the first object on the current page.
     *
     * @var ?string $endingBefore
     */
    #[SpeakeasyMetadata('queryParam:style=form,explode=true,name=ending_before')]
    public ?string $endingBefore = null;

    /**
     * Reservation status: `held` (the goods are on hold and do not count as available), `released` (the hold was undone and the goods are available again) and `consumed` (the hold ended by turning into a real movement). The last two are terminal. Exact match on `status`.
     *
     * @var ?\Factuarea\Sdk\Models\Operations\PublicApiV1StockReservationsListStatus $status
     */
    #[SpeakeasyMetadata('queryParam:style=form,explode=true,name=status')]
    public ?PublicApiV1StockReservationsListStatus $status = null;

    /**
     * Reservation status: `held` (the goods are on hold and do not count as available), `released` (the hold was undone and the goods are available again) and `consumed` (the hold ended by turning into a real movement). The last two are terminal. Comma-separated list. Any of the values matches.
     *
     * @var ?string $statusIn
     */
    #[SpeakeasyMetadata('queryParam:style=form,explode=true,name=status[in]')]
    public ?string $statusIn = null;

    /**
     * Kind of holder that keeps the goods on hold, the short name used when reserving (lowercase letters, digits and underscores, up to 40 characters). It is NOT a closed catalog: your own value coexists with the ones the platform writes. Exact match only, because a fragment would also match every future kind that starts the same way. Exact match on `holder_type`.
     *
     * @var ?string $holderType
     */
    #[SpeakeasyMetadata('queryParam:style=form,explode=true,name=holder_type')]
    public ?string $holderType = null;

    /**
     * Kind of holder that keeps the goods on hold, the short name used when reserving (lowercase letters, digits and underscores, up to 40 characters). It is NOT a closed catalog: your own value coexists with the ones the platform writes. Exact match only, because a fragment would also match every future kind that starts the same way. Comma-separated list. Any of the values matches.
     *
     * @var ?string $holderTypeIn
     */
    #[SpeakeasyMetadata('queryParam:style=form,explode=true,name=holder_type[in]')]
    public ?string $holderTypeIn = null;

    /**
     * Where the hold was created from (up to 40 characters). It is informative and stamped by the platform, never taken from the request body: a hold created through this API reads `public_api`. Exact match on `origin`.
     *
     * @var ?string $origin
     */
    #[SpeakeasyMetadata('queryParam:style=form,explode=true,name=origin')]
    public ?string $origin = null;

    /**
     * Where the hold was created from (up to 40 characters). It is informative and stamped by the platform, never taken from the request body: a hold created through this API reads `public_api`. Comma-separated list. Any of the values matches.
     *
     * @var ?string $originIn
     */
    #[SpeakeasyMetadata('queryParam:style=form,explode=true,name=origin[in]')]
    public ?string $originIn = null;

    /**
     * Moment the hold expires (ISO 8601). A hold created with no duration never expires and therefore never matches this filter; an expired hold stops counting against availability even before anything releases it. Exact match on `expires_at`.
     *
     * @var ?\DateTime $expiresAt
     */
    #[SpeakeasyMetadata('queryParam:style=form,explode=true,name=expires_at,dateTimeFormat=Y-m-d\TH:i:s.up')]
    public ?\DateTime $expiresAt = null;

    /**
     * Moment the hold expires (ISO 8601). A hold created with no duration never expires and therefore never matches this filter; an expired hold stops counting against availability even before anything releases it. Greater than or equal to the given value.
     *
     * @var ?\DateTime $expiresAtGte
     */
    #[SpeakeasyMetadata('queryParam:style=form,explode=true,name=expires_at[gte],dateTimeFormat=Y-m-d\TH:i:s.up')]
    public ?\DateTime $expiresAtGte = null;

    /**
     * Moment the hold expires (ISO 8601). A hold created with no duration never expires and therefore never matches this filter; an expired hold stops counting against availability even before anything releases it. Less than or equal to the given value.
     *
     * @var ?\DateTime $expiresAtLte
     */
    #[SpeakeasyMetadata('queryParam:style=form,explode=true,name=expires_at[lte],dateTimeFormat=Y-m-d\TH:i:s.up')]
    public ?\DateTime $expiresAtLte = null;

    /**
     * Moment the hold expires (ISO 8601). A hold created with no duration never expires and therefore never matches this filter; an expired hold stops counting against availability even before anything releases it. Strictly greater than the given value.
     *
     * @var ?\DateTime $expiresAtGt
     */
    #[SpeakeasyMetadata('queryParam:style=form,explode=true,name=expires_at[gt],dateTimeFormat=Y-m-d\TH:i:s.up')]
    public ?\DateTime $expiresAtGt = null;

    /**
     * Moment the hold expires (ISO 8601). A hold created with no duration never expires and therefore never matches this filter; an expired hold stops counting against availability even before anything releases it. Strictly less than the given value.
     *
     * @var ?\DateTime $expiresAtLt
     */
    #[SpeakeasyMetadata('queryParam:style=form,explode=true,name=expires_at[lt],dateTimeFormat=Y-m-d\TH:i:s.up')]
    public ?\DateTime $expiresAtLt = null;

    /**
     * Sort order. Use a field for ascending or a `-` prefix for descending (e.g. `-created`). Allowed fields: `held_at`, `expires_at`. Combined with the cursor, ordering stays deterministic (a stable secondary sort by the cursor id, Stripe-style). When omitted, results follow the default cursor order (`held_at` descending, the most recently held first).
     *
     * @var ?\Factuarea\Sdk\Models\Operations\PublicApiV1StockReservationsListSort $sort
     */
    #[SpeakeasyMetadata('queryParam:style=form,explode=true,name=sort')]
    public ?PublicApiV1StockReservationsListSort $sort = null;

    /**
     * Pin the API version (`YYYY-MM-DD`, Stripe-style date versioning) for this request; omit to use the key's pinned version, or the latest if none. Unsupported version → `400 unsupported_api_version`; malformed → `400 parameter_invalid_format`. The effective version is echoed in the `Factuarea-Version` response header. See the [Versioning guide](/guides/versioning).
     *
     * @var ?LocalDate $factuareaVersion
     */
    #[SpeakeasyMetadata('header:style=simple,explode=false,name=Factuarea-Version,dateTimeFormat=Y-m-d')]
    public ?LocalDate $factuareaVersion = null;

    /**
     * Operate on behalf of a child company (gestoría master key): pass its public `id` (UUID v7) and the request runs against that child's data without changing the key's scope, tier or environment (omit to use the key's own company). Invalid UUID → `400 parameter_invalid_uuid`; unknown or non-owned id → `404 profile_not_found`. See the [Acting on behalf guide](/guides/acting-on-behalf).
     *
     * @var ?string $xActiveProfile
     */
    #[SpeakeasyMetadata('header:style=simple,explode=false,name=X-Active-Profile')]
    public ?string $xActiveProfile = null;

    /**
     * Number of objects to return. Integer between 1 and 100. Defaults to 25.
     *
     * @var ?int $limit
     */
    #[SpeakeasyMetadata('queryParam:style=form,explode=true,name=limit')]
    public ?int $limit = null;

    /**
     * @param  string  $company
     * @param  ?string  $holderId
     * @param  ?string  $productId
     * @param  ?string  $variantId
     * @param  ?string  $warehouseId
     * @param  ?int  $limit
     * @param  ?string  $startingAfter
     * @param  ?string  $endingBefore
     * @param  ?\Factuarea\Sdk\Models\Operations\PublicApiV1StockReservationsListStatus  $status
     * @param  ?string  $statusIn
     * @param  ?string  $holderType
     * @param  ?string  $holderTypeIn
     * @param  ?string  $origin
     * @param  ?string  $originIn
     * @param  ?\DateTime  $expiresAt
     * @param  ?\DateTime  $expiresAtGte
     * @param  ?\DateTime  $expiresAtLte
     * @param  ?\DateTime  $expiresAtGt
     * @param  ?\DateTime  $expiresAtLt
     * @param  ?\Factuarea\Sdk\Models\Operations\PublicApiV1StockReservationsListSort  $sort
     * @param  ?LocalDate  $factuareaVersion
     * @param  ?string  $xActiveProfile
     * @phpstan-pure
     */
    public function __construct(string $company, ?string $holderId = null, ?string $productId = null, ?string $variantId = null, ?string $warehouseId = null, ?string $startingAfter = null, ?string $endingBefore = null, ?PublicApiV1StockReservationsListStatus $status = null, ?string $statusIn = null, ?string $holderType = null, ?string $holderTypeIn = null, ?string $origin = null, ?string $originIn = null, ?\DateTime $expiresAt = null, ?\DateTime $expiresAtGte = null, ?\DateTime $expiresAtLte = null, ?\DateTime $expiresAtGt = null, ?\DateTime $expiresAtLt = null, ?PublicApiV1StockReservationsListSort $sort = null, ?LocalDate $factuareaVersion = null, ?string $xActiveProfile = null, ?int $limit = 25)
    {
        $this->company = $company;
        $this->holderId = $holderId;
        $this->productId = $productId;
        $this->variantId = $variantId;
        $this->warehouseId = $warehouseId;
        $this->startingAfter = $startingAfter;
        $this->endingBefore = $endingBefore;
        $this->status = $status;
        $this->statusIn = $statusIn;
        $this->holderType = $holderType;
        $this->holderTypeIn = $holderTypeIn;
        $this->origin = $origin;
        $this->originIn = $originIn;
        $this->expiresAt = $expiresAt;
        $this->expiresAtGte = $expiresAtGte;
        $this->expiresAtLte = $expiresAtLte;
        $this->expiresAtGt = $expiresAtGt;
        $this->expiresAtLt = $expiresAtLt;
        $this->sort = $sort;
        $this->factuareaVersion = $factuareaVersion;
        $this->xActiveProfile = $xActiveProfile;
        $this->limit = $limit;
    }
}