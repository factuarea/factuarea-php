<?php

/**
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */


declare(strict_types=1);

namespace Factuarea\Sdk\Hooks;


/**
 * Available scopes for the OAuth2 OAuth 2.0 scheme (authorizationCode flow).
 *
 * OAuth 2.1 authorization-code flow with PKCE (`code_challenge_method=S256`).
 * Discovery metadata (RFC 8414) is published at `/.well-known/oauth-authorization-server`.
 *
 * ## API key rotation policy
 *
 * API keys and OAuth client secrets are long-lived credentials and MUST be rotated periodically and immediately after any suspected leak.
 *
 * - **Prefixes**: every key carries an unambiguous environment prefix — `fact_live_` (production) and `fact_test_` (sandbox). The prefix is the source of truth for the environment; never mix them across environments.
 * - **How to rotate**: rotate an API key from the Factuarea portal (or via the self-service `account:write` endpoints). Rotation issues a brand-new secret and returns it **once** — store it immediately, it is never shown again.
 * - **Grace window (dual-secret)**: after a rotation the previous secret keeps working for a **24-hour grace window**, so you can roll out the new secret with zero downtime. During that window both the new and the previous secret are accepted; once the window expires the previous secret is rejected and purged.
 * - **When to rotate**: on a regular schedule (e.g. every 90 days), whenever a teammate with access leaves, and **immediately** if a secret is ever exposed in logs, source control or a public client.
 * - **What a key reaches**: an API key belongs to one account and operates only on the companies (tax IDs) in its scope, fixed when the key is issued; `GET /v1/me` returns the account (`data.account.id`) and that scope (`data.scope[]`). The company each request acts on travels in the path, `/v1/companies/{company}/…`, and must be in the scope.
 * - Secrets MUST NEVER be embedded in browsers, mobile apps or any public client — keep them server-side only.
 */
enum OAuth2Scope: string
{

    case ContactsRead = 'contacts:read';

    case ContactsWrite = 'contacts:write';

    case ContactsDelete = 'contacts:delete';

    case ProductsRead = 'products:read';

    case ProductsWrite = 'products:write';

    case ProductsDelete = 'products:delete';

    case PriceListsRead = 'price_lists:read';

    case PriceListsWrite = 'price_lists:write';

    case InvoicesRead = 'invoices:read';

    case InvoicesWrite = 'invoices:write';

    case InvoicesDelete = 'invoices:delete';

    case InvoicesSend = 'invoices:send';

    case InvoicesVoid = 'invoices:void';

    case QuotesRead = 'quotes:read';

    case QuotesWrite = 'quotes:write';

    case QuotesDelete = 'quotes:delete';

    case QuotesSend = 'quotes:send';

    case QuotesTransition = 'quotes:transition';

    case ProformasRead = 'proformas:read';

    case ProformasWrite = 'proformas:write';

    case ProformasDelete = 'proformas:delete';

    case ProformasSend = 'proformas:send';

    case ProformasTransition = 'proformas:transition';

    case DeliveryNotesRead = 'delivery_notes:read';

    case DeliveryNotesWrite = 'delivery_notes:write';

    case DeliveryNotesDelete = 'delivery_notes:delete';

    case DeliveryNotesTransition = 'delivery_notes:transition';

    case DeliveryNotesGdprForget = 'delivery_notes:gdpr_forget';

    case PurchaseInvoicesRead = 'purchase_invoices:read';

    case PurchaseInvoicesWrite = 'purchase_invoices:write';

    case PurchaseInvoicesDelete = 'purchase_invoices:delete';

    case PurchaseInvoicesTransition = 'purchase_invoices:transition';

    case RecurringInvoicesRead = 'recurring_invoices:read';

    case RecurringInvoicesWrite = 'recurring_invoices:write';

    case RecurringInvoicesDelete = 'recurring_invoices:delete';

    case RecurringInvoicesTransition = 'recurring_invoices:transition';

    case TaxesRead = 'taxes:read';

    case TaxesWrite = 'taxes:write';

    case TaxesDelete = 'taxes:delete';

    case SeriesRead = 'series:read';

    case SeriesWrite = 'series:write';

    case PdfsRead = 'pdfs:read';

    case WebhooksRead = 'webhooks:read';

    case WebhooksWrite = 'webhooks:write';

    case WebhooksDelete = 'webhooks:delete';

    case EventsRead = 'events:read';

    case VerifactuRead = 'verifactu:read';

    case VerifactuWrite = 'verifactu:write';

    case FacturaeRead = 'facturae:read';

    case FacturaeWrite = 'facturae:write';

    case TaxReportsRead = 'tax_reports:read';

    case TaxReportsWrite = 'tax_reports:write';

    case AccountRead = 'account:read';

    case AccountWrite = 'account:write';

    case AccountOwner = 'account:owner';

    case CompaniesRead = 'companies:read';

    case CompaniesWrite = 'companies:write';

    case CompaniesDelete = 'companies:delete';

    case ApiKeysRead = 'api_keys:read';

    case ApiKeysWrite = 'api_keys:write';

    case ApiKeysDelete = 'api_keys:delete';

    case StripeAutoinvoicingRead = 'stripe_autoinvoicing:read';

    case StripeAutoinvoicingWrite = 'stripe_autoinvoicing:write';

    case PayoutsRead = 'payouts:read';

    case GocardlessAutoinvoicingRead = 'gocardless_autoinvoicing:read';

    case GocardlessAutoinvoicingWrite = 'gocardless_autoinvoicing:write';

    case MoneiAutoinvoicingRead = 'monei_autoinvoicing:read';

    case MoneiAutoinvoicingWrite = 'monei_autoinvoicing:write';

    case WoocommerceStoreRead = 'woocommerce_store:read';

    case WoocommerceStoreWrite = 'woocommerce_store:write';

    case ShopifyStoreRead = 'shopify_store:read';

    case ShopifyStoreWrite = 'shopify_store:write';

    case PrestashopStoreRead = 'prestashop_store:read';

    case PrestashopStoreWrite = 'prestashop_store:write';

    case StoresRead = 'stores:read';

    case StoresWrite = 'stores:write';

    case EmployeesRead = 'employees:read';

    case EmployeesWrite = 'employees:write';

    case EmployeesDelete = 'employees:delete';

    case TimeEntriesRead = 'time_entries:read';

    case TimeEntriesWrite = 'time_entries:write';

    case AbsencesRead = 'absences:read';

    case AbsencesWrite = 'absences:write';

    case AbsencesTransition = 'absences:transition';

    case WorkSchedulesRead = 'work_schedules:read';

    case WorkSchedulesWrite = 'work_schedules:write';

    case PresenceRead = 'presence:read';

    case HolidaysRead = 'holidays:read';

    case PayrollExportsRead = 'payroll_exports:read';

    case PayrollExportsWrite = 'payroll_exports:write';

    case DevelopersRead = 'developers:read';

    case EmailsRead = 'emails:read';

    case IntegrationEventsRead = 'integration_events:read';

    case IntegrationEventsWrite = 'integration_events:write';

    case AutomationsRead = 'automations:read';

    case AutomationsWrite = 'automations:write';

    case AutomationsDelete = 'automations:delete';

    case AutomationRunsRead = 'automation_runs:read';

    case MembersRead = 'members:read';

    case MembersWrite = 'members:write';

    case InvitationsRead = 'invitations:read';

    case InvitationsWrite = 'invitations:write';

    case ClaimTokensRead = 'claim_tokens:read';

    case ClaimTokensWrite = 'claim_tokens:write';

    case Wildcard = '*';
}
