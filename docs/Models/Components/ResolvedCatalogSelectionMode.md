# ResolvedCatalogSelectionMode

`open` for explicit open availability, `allow_list` for explicit closed availability. `legacy` is retained only for backwards-compatible historical payloads.


## Values

| Name        | Value       |
| ----------- | ----------- |
| `Open`      | open        |
| `AllowList` | allow_list  |
| `Legacy`    | legacy      |