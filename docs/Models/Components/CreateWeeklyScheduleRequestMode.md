# CreateWeeklyScheduleRequestMode

Schedule mode: `validated` (assumed fulfilled) or `real_clocking` (computed from real clock entries).


## Values

| Name           | Value          |
| -------------- | -------------- |
| `Validated`    | validated      |
| `RealClocking` | real_clocking  |