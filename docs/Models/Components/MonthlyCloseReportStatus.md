# MonthlyCloseReportStatus

State of the close (`closed`/`reopened`), so a reopened close is not mistaken for immutable.


## Values

| Name       | Value      |
| ---------- | ---------- |
| `Closed`   | closed     |
| `Reopened` | reopened   |