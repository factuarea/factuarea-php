# StripeAutoinvoicingConfigObject

Stripe-like discriminator. Always `stripe_autoinvoicing_config` for this resource.


## Values

| Name                        | Value                       |
| --------------------------- | --------------------------- |
| `StripeAutoinvoicingConfig` | stripe_autoinvoicing_config |