# MeEnvironment

Environment of the authenticated key: `live` (`fact_live_` prefix, real side effects) or `test` (`fact_test_` prefix, sandbox company, no real-world effects). Always matches the real prefix of the credential.


## Values

| Name   | Value  |
| ------ | ------ |
| `Live` | live   |
| `Test` | test   |