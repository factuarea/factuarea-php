# ImportClientsV1Request

Import clients from a file as `multipart/form-data`. `file` is a CSV/XLSX/XLS/ODS/TXT document (≤10 MB); `mapping` maps your column headers to target fields and must include at least `name` and `tax_id`; `dry_run` (default `false`) validates the file and returns a per-row preview without persisting.


## Fields

| Field                                                                                                           | Type                                                                                                            | Required                                                                                                        | Description                                                                                                     |
| --------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------- |
| `file`                                                                                                          | [Components\ImportClientsV1RequestFile](../../Models/Components/ImportClientsV1RequestFile.md)                  | :heavy_check_mark:                                                                                              | CSV/XLSX/XLS/ODS/TXT file with the clients to import (max 10 MB).                                               |
| `dryRun`                                                                                                        | *?bool*                                                                                                         | :heavy_minus_sign:                                                                                              | If `true`, validates the file and returns a per-row preview WITHOUT persisting any client. Defaults to `false`. |
| `mapping`                                                                                                       | array<string, *string*>                                                                                         | :heavy_check_mark:                                                                                              | Mapeo de columnas `{cabecera_csv: campo_destino}`. Debe declarar al<br/>menos `name` y `tax_id`.                |