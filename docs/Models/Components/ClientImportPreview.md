# ClientImportPreview

Validate-only preview of a client import (`POST /v1/clients/import` with `dry_run=true`). The CSV/XLSX file is checked row by row without persisting anything: `rows[]` carries the per-row classification `{row, status, errors[], warnings[]}` identified by the 1-based file `row` number (the header is row 1).


## Fields

| Field                                                                                        | Type                                                                                         | Required                                                                                     | Description                                                                                  | Example                                                                                      |
| -------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------- |
| `object`                                                                                     | [Components\ClientImportPreviewObject](../../Models/Components/ClientImportPreviewObject.md) | :heavy_check_mark:                                                                           | Stripe-like discriminator. Always `client_import_preview` for this resource.                 | client_import_preview                                                                        |
| `totalRows`                                                                                  | *int*                                                                                        | :heavy_check_mark:                                                                           | Number of data rows in the uploaded file (excluding the header).                             |                                                                                              |
| `rows`                                                                                       | array<[Components\Row](../../Models/Components/Row.md)>                                      | :heavy_check_mark:                                                                           | Per-row validation of the file. One item per data row, in file order.                        |                                                                                              |