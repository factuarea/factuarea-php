# PlatformRole

Global platform role of the person. A DIFFERENT vocabulary from `roles[].membership_role`: the same concept is called `user` here and `member` there.


## Values

| Name       | Value      |
| ---------- | ---------- |
| `Admin`    | admin      |
| `User`     | user       |
| `Employee` | employee   |