# ConnectStoreV1Request

Public REST API v1 — POST /v1/companies/{company}/stores.

Connect an e-commerce store to your company. Required: `integration_id` (the
provider connection that backs the store), `provider`, `external_store_id`
(the identifier the provider gives to the shop) and `name`. The eight
settings are optional and each one falls back to its declared default, so a
store connected without any of them is created with automatic invoicing OFF
and in the `test` environment. `simplified_threshold` is expressed in EUROS
and capped at the legal maximum for a simplified invoice. `series_id` is a
series of type invoice; omit it to follow the default series of your company,
which is never frozen into the store. A company may connect several stores of
the same provider, but not the same remote shop twice.


## Fields

| Field                                                                                                       | Type                                                                                                        | Required                                                                                                    | Description                                                                                                 |
| ----------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------- |
| `integrationId`                                                                                             | *string*                                                                                                    | :heavy_check_mark:                                                                                          | Identifier (UUID v7) of the provider connection that backs the store.                                       |
| `provider`                                                                                                  | [Components\ConnectStoreV1RequestProvider](../../Models/Components/ConnectStoreV1RequestProvider.md)        | :heavy_check_mark:                                                                                          | E-commerce provider of the store: `woocommerce`, `shopify` or `prestashop`.                                 |
| `externalStoreId`                                                                                           | *string*                                                                                                    | :heavy_check_mark:                                                                                          | Identifier the provider gives to the shop; unique per provider within your company.                         |
| `name`                                                                                                      | *string*                                                                                                    | :heavy_check_mark:                                                                                          | Display name of the store, editable at any time.                                                            |
| `seriesId`                                                                                                  | *?string*                                                                                                   | :heavy_minus_sign:                                                                                          | Series (UUID v7, type invoice) the store numbers into; omit to follow the default series of your company.   |
| `simplifiedThreshold`                                                                                       | *?float*                                                                                                    | :heavy_minus_sign:                                                                                          | Order total, in euros, up to which the store issues a simplified invoice. Maximum 3000.00.                  |
| `requireTaxId`                                                                                              | *?bool*                                                                                                     | :heavy_minus_sign:                                                                                          | Whether the buyer tax ID is required for the order to be invoiced.                                          |
| `pricesIncludeTax`                                                                                          | *?bool*                                                                                                     | :heavy_minus_sign:                                                                                          | Whether the prices of the store already include taxes.                                                      |
| `autoinvoicingEnabled`                                                                                      | *?bool*                                                                                                     | :heavy_minus_sign:                                                                                          | Whether paid orders are invoiced automatically. Off unless you turn it on.                                  |
| `autosendEnabled`                                                                                           | *?bool*                                                                                                     | :heavy_minus_sign:                                                                                          | Whether the invoice is emailed to the buyer automatically. Off unless you turn it on.                       |
| `remoteBaseUrl`                                                                                             | *?string*                                                                                                   | :heavy_minus_sign:                                                                                          | Base address of the remote shop, checked against the outbound policy of your company.                       |
| `environment`                                                                                               | [?Components\ConnectStoreV1RequestEnvironment](../../Models/Components/ConnectStoreV1RequestEnvironment.md) | :heavy_minus_sign:                                                                                          | Environment the store reports as: `live` invoices for real, `test` discards with a typed reason.            |
| `authorizeRemoteHost`                                                                                       | *?bool*                                                                                                     | :heavy_minus_sign:                                                                                          | Authorize the host of `remote_base_url` as an outbound destination in the same call.                        |