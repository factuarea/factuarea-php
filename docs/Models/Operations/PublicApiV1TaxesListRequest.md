# PublicApiV1TaxesListRequest


## Fields

| Field                                                                                                | Type                                                                                                 | Required                                                                                             | Description                                                                                          |
| ---------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------- |
| `limit`                                                                                              | *?int*                                                                                               | :heavy_minus_sign:                                                                                   | Number of objects to return. Integer between 1 and 100. Defaults to 25.                              |
| `startingAfter`                                                                                      | *?string*                                                                                            | :heavy_minus_sign:                                                                                   | Cursor for forward pagination. Use the `uuid` of the last object on the previous page.               |
| `endingBefore`                                                                                       | *?string*                                                                                            | :heavy_minus_sign:                                                                                   | Cursor for backward pagination. Use the `uuid` of the first object on the current page.              |
| `type`                                                                                               | *?string*                                                                                            | :heavy_minus_sign:                                                                                   | Tax type: `vat`, `retention`, `surcharge`, `other`. Exact match on `type`.                           |
| `typeIn`                                                                                             | *?string*                                                                                            | :heavy_minus_sign:                                                                                   | Tax type: `vat`, `retention`, `surcharge`, `other`. Comma-separated list. Any of the values matches. |
| `isActive`                                                                                           | *?bool*                                                                                              | :heavy_minus_sign:                                                                                   | Filter active / inactive taxes. Exact match on `is_active`.                                          |
| `isDefault`                                                                                          | *?bool*                                                                                              | :heavy_minus_sign:                                                                                   | Filter taxes marked as global default (legacy flag). Exact match on `is_default`.                    |
| `appliesTo`                                                                                          | *?string*                                                                                            | :heavy_minus_sign:                                                                                   | Scope: `sales`, `purchases`, `both`. Exact match on `applies_to`.                                    |
| `appliesToIn`                                                                                        | *?string*                                                                                            | :heavy_minus_sign:                                                                                   | Scope: `sales`, `purchases`, `both`. Comma-separated list. Any of the values matches.                |
| `country`                                                                                            | *?string*                                                                                            | :heavy_minus_sign:                                                                                   | ISO 3166-1 alpha-2 country code. Exact match on `country`.                                           |
| `code`                                                                                               | *?string*                                                                                            | :heavy_minus_sign:                                                                                   | Short tax code (exact match). Exact match on `code`.                                                 |
| `search`                                                                                             | *?string*                                                                                            | :heavy_minus_sign:                                                                                   | Escaped LIKE search over `name` and `code` (limit 80 chars). Exact match on `search`.                |
| `externalReference`                                                                                  | *?string*                                                                                            | :heavy_minus_sign:                                                                                   | Exact AEAT key: S1..S3, E1..E6, N1..N2. Exact match on `external_reference`.                         |
| `externalReferenceIn`                                                                                | *?string*                                                                                            | :heavy_minus_sign:                                                                                   | Exact AEAT key: S1..S3, E1..E6, N1..N2. Comma-separated list. Any of the values matches.             |
| `isSystem`                                                                                           | *?bool*                                                                                              | :heavy_minus_sign:                                                                                   | Filter system taxes (global catalog) vs custom. Exact match on `is_system`.                          |